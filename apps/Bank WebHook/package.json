{
  "name": "@ayush696954/bank-webhook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --format=esm",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.0",
    "cors": "^2.8.5",
    "esbuild": "^0.25.1",
    "esbuild-loader": "^4.3.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.19.3"
  }
}
